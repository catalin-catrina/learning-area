import { ImageLinkPipe } from './image-link.pipe';

describe('ImageLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
