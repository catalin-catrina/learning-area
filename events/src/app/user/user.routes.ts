import { ProfileComponent } from './profile/profile.component';
import { Routes } from '@angular/router';

// path is user/profile
export const userRoutes: Routes = [
  { path: 'profile', component: ProfileComponent },
];
